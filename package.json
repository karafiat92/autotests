{
  "devDependencies": {
    "@shelex/cypress-allure-plugin": "^2.28.0",
    "cypress": "^10.3.0"
  },
  "dependencies": {
    "bignumber.js": "^9.0.2",
    "npm": "^8.13.2"
  },
  "scripts": {
    "open": "npx cypress open",
    "test": "del allure-results /S /Q \nnpx cypress run --browser chrome --env allure=true",
    "test:stable": "npx cypress run --browser chrome --config baseUrl=https://stable-web.tezro.com/"
  }
}
